spring.application.name=sequia_integra
server.port = 8080

# Database config.
spring.data.mongodb.host = localhost
spring.data.mongodb.port = 27017
spring.data.mongodb.database = integra

# Swagger config.
springdoc.swagger-ui.path = /swagger-ui
